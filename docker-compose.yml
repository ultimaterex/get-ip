services:
  get-ip:
    build: .
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      PORT: "8080"
      GEOLITE_CITY_PATH: /data/GeoLite2-City.mmdb
      MAXMIND_ACCOUNT_ID: ${MAXMIND_ACCOUNT_ID:-}
      MAXMIND_LICENSE_KEY: ${MAXMIND_LICENSE_KEY:-}
      GEOLITE_MAX_AGE_DAYS: ${GEOLITE_MAX_AGE_DAYS:-7}
    volumes:
      - get-ip-data:/data
    restart: unless-stopped

volumes:
  get-ip-data:
